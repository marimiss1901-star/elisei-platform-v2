ELISEI 5.13.0 PATCH MANIFEST
Base: ELISEI 5.12.0 + Password Reset hotfix
Frontend: 5.13.0
Backend: 2.25.0
El Core: 5.13.0

Purpose: Daily Ready — yesterday is prepared before login.

Core rules:
- User entry must not launch WB synchronization.
- Yesterday is a persisted database snapshot, not a live loading screen.
- Operational polling uses moderate cadence; no 2–5 minute API storm.
- Heavy financial stages use a separate scheduler lane and respect WB next_allowed_at.
- Missing/partial data never becomes a false zero.
- Manual sync remains available only as diagnostics.
- Free Render is woken at three morning checkpoints by GitHub Actions; wake itself does not force every WB stage.

Regression: 39/39 backend/frontend source tests passed.
JS/CJS syntax: passed.
JSX TypeScript parser: passed.
YAML parse: passed.

Payload files: 20 (including release notes; excluding checksum/manifest helpers).
Archive limit: < 100 files.
