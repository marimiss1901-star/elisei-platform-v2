ELISEI 5.6.0 — PATCH FROM 5.5.0

Назначение: полный финансовый контур Wildberries — официальные сводки реализации и эквайринга, дневная динамика, замеры склада и расшифровка причин удержаний.
Применение: распаковать папку патча и скопировать её содержимое в корень репозитория ELISEI 5.5.0 с заменой файлов.
MAXADORRE не затрагивается.

Важно:
- FILES.txt — инвентарный список контролируемых файлов репозитория.
- PATCH_FILES.txt — точный перечень файлов проекта, которые меняет этот патч.
- Новые сводки используются для сверки и не прибавляются в P&L повторно.
- Самовыкупы и маркировка сохраняются как детализация причин удержаний без двойного вычитания.
- Незавершённый пустой источник отображается как «Ожидает WB», а не как нулевая сумма.
- Списки финансовых отчётов в облачном ELISEI требуют Сервисный токен и WB_CLIENT_SECRET.

Проверено:
- backend npm test: 8/8 наборов пройдены;
- node --check для всех backend/src/*.js;
- синтаксическая проверка 12 frontend JS/JSX-файлов через TypeScript parser;
- попытка production-сборки остановлена недоступностью lucide-react во внутреннем npm-реестре среды (HTTP 404), не ошибкой исходного кода;
- наложение патча на чистую 5.5.0 и побайтовая сверка изменяемых файлов;
- целостность ZIP и SHA-256.

Количество изменяемых файлов проекта: 15.
Общее количество файлов внутри папки патча, включая служебные: 18.
