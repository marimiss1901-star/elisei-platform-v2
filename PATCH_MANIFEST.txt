ELISEI 5.9.0 PATCH FROM 5.8.0

Purpose:
- add safe frequent WB polling through the existing API token without seller-cabinet browser automation;
- prepare OAuth readiness without enabling an unverified OAuth code exchange;
- add official WB webhook storage, secret verification, seller validation and event idempotency for registered catalog services;
- process up to 100 webhook events with one PostgreSQL batch and acknowledge before heavy synchronization;
- keep webhook-triggered work paused when the user disables live mode;
- recover STOCK_HISTORY_DAILY_CSV tasks after deploy, timeout or a duplicate report UUID;
- preserve finance/document limits and all 5.8.0 financial behavior.

Install over: ELISEI 5.8.0
Changed project files: 14
