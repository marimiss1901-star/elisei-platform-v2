ELISEI 5.7.6 PATCH FROM 5.7.5

Purpose:
- route FBS/FBO questions to the sales module even when conversation metadata is temporarily unavailable;
- calculate FBS/FBO directly from raw order rows for the selected period;
- keep unknown fulfillment rows separate without double counting or false zeroes;
- preserve resolvedPeriod, analysisContext and modulesUsed in frontend history;
- show the actual backend El engine version in the interface;
- replace generic connection fallback with a targeted, truthful order-split response.

Install over: ELISEI 5.7.5
Changed project files: 13
