ELISEI 5.7.5 PATCH FROM 5.7.4

Purpose:
- conversational follow-ups inherit the previous period and business topic;
- short questions such as “из них”, “а сколько FBS?”, “а возвратов?” receive focused answers;
- FBS/FBO order counts are calculated from order rows without double counting;
- orders with an unknown fulfillment scheme remain in a separate unknown bucket;
- missing scheme detail is never displayed as a false FBS/FBO zero.

Install over: ELISEI 5.7.4
Changed project files: 11
