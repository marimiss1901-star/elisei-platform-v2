ELISEI 5.10.3 / Backend 2.22.3 / El Core 5.10.3

HOTFIX
- Исправлена миграция старого длинного nextAllowedAt у финансов WB.
- 5.10.2 ошибочно зависел от текста «12 часов» в metadata; в живом состоянии кабинета этот текст мог отсутствовать или лежать в другом поле.
- Теперь незавершённый finance со статусом queued, уже сохранёнными строками и паузой более 2 минут автоматически считается старым некорректным continuation и возобновляется немедленно.
- Реальные ответы WB 429 не сбрасываются: они имеют status=rate_limited и продолжают соблюдать Retry-After.
- Восстановление выполняется сразу при старте backend, а также при обычном background-worker и чтении подключения.
- rrdId, syncId, last_count и уже сохранённые строки не обнуляются.

TESTS
- 28/28 backend test suites passed.
