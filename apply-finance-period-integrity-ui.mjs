import fs from 'node:fs'

// ELISEI 5.19.14 — Finance page selected-period integrity.
// Never label a previous-period analytics/ledger cache as the currently selected
// period. Confirmed-only headline cards must not be synthesized from revenue or
// fallback costs when the selected-period WB finance register is still missing.
const file='src/pages/DashboardPage.jsx'
let source=fs.readFileSync(file,'utf8')

function replaceOnce(before,after,label){
  if(source.includes(after)) return
  if(!source.includes(before)) throw new Error(`ELISEI 5.19.14 finance period integrity: ${label} marker not found`)
  source=source.replace(before,after)
}

replaceOnce(
`  const renderFinance = () => {\n    const basePeriodFinanceSummary = analyticsCore?.summary || summary || {}\n    const periodFinanceRows = analyticsBaseProducts.length ? analyticsBaseProducts : productRows\n    const financeProductNeedle = query.trim().toLowerCase()\n    const visibleFinanceProducts = periodFinanceRows.filter(row => !financeProductNeedle || [row.article,row.vendorCode,row.barcode,row.title,row.brand,row.nmID,row.key].join(' ').toLowerCase().includes(financeProductNeedle))\n    const ledger = financeLedger || { rows:[],summary:{},products:[],groups:[],sources:[],timeline:[],reports:{sales:{rows:[]},acquiring:{rows:[]}},riskDetails:{},coverage:{} }\n    const ledgerRows = Array.isArray(ledger.rows) ? ledger.rows : []\n    const rawLedgerSummary = ledger.summary || {}`,
`  const renderFinance = () => {\n    const financeAnalyticsPeriodAligned = analyticsPeriodsMatch(analyticsVisiblePeriod,analyticsPeriod)\n    const basePeriodFinanceSummary = financeAnalyticsPeriodAligned ? (analyticsCore?.summary || {}) : {}\n    const periodFinanceRows = financeAnalyticsPeriodAligned ? (analyticsBaseProducts.length ? analyticsBaseProducts : productRows) : []\n    const financeProductNeedle = query.trim().toLowerCase()\n    const visibleFinanceProducts = periodFinanceRows.filter(row => !financeProductNeedle || [row.article,row.vendorCode,row.barcode,row.title,row.brand,row.nmID,row.key].join(' ').toLowerCase().includes(financeProductNeedle))\n    const emptyFinanceLedger = { rows:[],summary:{},products:[],groups:[],sources:[],timeline:[],reports:{sales:{rows:[]},acquiring:{rows:[]}},riskDetails:{},coverage:{} }\n    const rawFinanceLedger = financeLedger || emptyFinanceLedger\n    const financeLedgerSelectedPeriod = rawFinanceLedger?.coverage?.selectedPeriod || null\n    const financeLedgerPeriodAligned = !financeLedger || analyticsPeriodsMatch(financeLedgerSelectedPeriod,analyticsPeriod)\n    const ledger = financeLedgerPeriodAligned ? rawFinanceLedger : emptyFinanceLedger\n    const ledgerRows = Array.isArray(ledger.rows) ? ledger.rows : []\n    const rawLedgerSummary = ledger.summary || {}`,
'finance analytics and ledger period guards')

replaceOnce(
`    const ledgerSummary = ledgerHasMovements || financeScopeFiltered ? rawLedgerSummary : {\n      ...rawLedgerSummary,\n      sellerPayable:Number(basePeriodFinanceSummary.revenue || 0),\n      expenses:provisionalWbExpenses,\n      commission:Number(basePeriodFinanceSummary.commission || 0),\n      logistics:Number(basePeriodFinanceSummary.logistics || 0),\n      storage:Number(basePeriodFinanceSummary.storage || 0),\n      acceptance:Number(basePeriodFinanceSummary.acceptance || 0),\n      acquiring:Number(basePeriodFinanceSummary.acquiring || 0),\n      penalties:Number(basePeriodFinanceSummary.penalties || 0),\n      deductions:Number(basePeriodFinanceSummary.deductions || 0),\n      compensations:Number(basePeriodFinanceSummary.additionalPayment || 0),\n    }`,
`    const ledgerSummary = ledgerHasMovements || financeScopeFiltered ? rawLedgerSummary : {\n      ...rawLedgerSummary,\n      // forPay, volatile retentions and compensations are confirmed-only fields.\n      // Revenue/fallback P&L must never impersonate the WB financial register.\n      sellerPayable:null,\n      expenses:provisionalWbExpenses,\n      commission:Number(basePeriodFinanceSummary.commission || 0),\n      logistics:Number(basePeriodFinanceSummary.logistics || 0),\n      storage:Number(basePeriodFinanceSummary.storage || 0),\n      acceptance:Number(basePeriodFinanceSummary.acceptance || 0),\n      acquiring:Number(basePeriodFinanceSummary.acquiring || 0),\n      penalties:null,\n      deductions:null,\n      compensations:null,\n    }`,
'confirmed-only finance fallback fields')

replaceOnce(
`    const financeReady = Boolean(ledgerHasMovements || selectedPeriodCovered || (!ledger.coverage?.selectedPeriod && (ledger.coverage?.financeReady || coreData?.availability?.finance)))`,
`    const financeReady = Boolean(financeLedgerPeriodAligned && (ledgerHasMovements || selectedPeriodCovered))`,
'finance readiness must belong to selected period')

replaceOnce(
`    const statusText = financeScopeFiltered && !ledgerHasMovements\n      ? \`По фильтру «\${financeScopeLabel}» подтверждённых операций пока нет. Итоги всего кабинета сюда не подставляются.\``,
`    const financePeriodMismatch = !financeAnalyticsPeriodAligned || !financeLedgerPeriodAligned\n    const statusText = !financeAnalyticsPeriodAligned\n      ? \`Расчёт за \${formatDate(analyticsPeriod.from)} — \${formatDate(analyticsPeriod.to)} ещё не получен. ELISEI не подставляет цифры другого периода и автоматически повторяет чтение.\`\n      : !financeLedgerPeriodAligned\n        ? \`Финансовый реестр на экране относился к другому периоду. Он скрыт; ждём реестр WB за \${formatDate(analyticsPeriod.from)} — \${formatDate(analyticsPeriod.to)}.\`\n      : financeScopeFiltered && !ledgerHasMovements\n      ? \`По фильтру «\${financeScopeLabel}» подтверждённых операций пока нет. Итоги всего кабинета сюда не подставляются.\``,
'stale period status copy')

replaceOnce(
`    const financeValue = value => financeScopeFiltered && !ledgerHasMovements ? 'Нет операций' : financeComplete || ledgerHasMovements || financeEstimateAvailable || Number(value || 0) !== 0 ? formatMoney(value) : 'Ожидает WB'\n    const reportNumber = (row, aliases = []) => {`,
`    const financeValue = value => {\n      if (value == null) return financeScopeFiltered && !ledgerHasMovements ? 'Нет операций' : 'Ожидает WB'\n      return financeScopeFiltered && !ledgerHasMovements ? 'Нет операций' : financeComplete || ledgerHasMovements || financeEstimateAvailable || Number(value || 0) !== 0 ? formatMoney(value) : 'Ожидает WB'\n    }\n    const financeBalance = financeAnalyticsPeriodAligned ? analyticsCore?.finance?.balance : null\n    const headlineSellerPayable = ledgerHasMovements ? Number(ledgerSummary.sellerPayable || 0) : null\n    const headlineExpenses = ledgerHasMovements ? Number(ledgerSummary.expenses || 0) : financeEstimateAvailable ? provisionalWbExpenses : null\n    const headlineLogistics = ledgerHasMovements ? Number(ledgerSummary.logistics || 0) : financeEstimateAvailable ? Number(basePeriodFinanceSummary.logistics || 0) : null\n    const headlineRetentions = ledgerHasMovements\n      ? (financePartial && Number(ledgerSummary.penalties || 0) === 0 && Number(ledgerSummary.deductions || 0) === 0 ? null : Number(ledgerSummary.penalties || 0)+Number(ledgerSummary.deductions || 0))\n      : null\n    const headlineCompensations = ledgerHasMovements\n      ? (financePartial && Number(ledgerSummary.compensations || 0) === 0 ? null : Number(ledgerSummary.compensations || 0))\n      : null\n    const reportNumber = (row, aliases = []) => {`,
'finance headline truthfulness helpers')

replaceOnce(
`      <div className={\`notice \${financeComplete ? 'success' : 'warning'}\`}><ShieldCheck size={20}/><div><strong>{financeComplete ? 'Финансовые данные подтверждены WB' : financeReady ? 'Финансовые данные загружены частично' : financeEstimateAvailable ? 'Предварительный расчёт доступен' : 'Ожидает «Финансы WB»'}</strong><p>{statusText}</p></div><button onClick={() => setActive('Синхронизации')}>Открыть статусы</button></div>`,
`      <div className={\`notice \${financeComplete ? 'success' : 'warning'}\`}><ShieldCheck size={20}/><div><strong>{financePeriodMismatch ? 'Ждём данные выбранного периода' : financeComplete ? 'Финансовые данные подтверждены WB' : financeReady ? 'Финансовые данные загружены частично' : financeEstimateAvailable ? 'Предварительный расчёт доступен' : 'Ожидает «Финансы WB»'}</strong><p>{statusText}</p></div><button onClick={() => setActive('Синхронизации')}>Открыть статусы</button></div>`,
'finance period mismatch notice')

replaceOnce(
`        <MetricCard label=\"К перечислению\" value={financeValue(ledgerSummary.sellerPayable)} delta=\"поле forPay из отчёта WB · выбранный период\" icon={WalletCards}/>\n        <MetricCard label=\"Доступно к выводу\" value={analyticsCore?.finance?.balance?.for_withdraw == null ? 'Ожидается' : formatMoney(analyticsCore.finance.balance.for_withdraw)} delta=\"текущий снимок WB · не зависит от выбранного периода\" icon={WalletCards}/>\n        <MetricCard label=\"Баланс WB\" value={analyticsCore?.finance?.balance?.current == null ? 'Ожидается' : formatMoney(analyticsCore.finance.balance.current)} delta={analyticsCore?.finance?.balance?.updatedAt ? \`обновлено \${formatLocalDateTime(analyticsCore.finance.balance.updatedAt)}\` : 'ночной снимок ещё не загружен'} icon={CircleDollarSign}/>\n        <MetricCard label=\"Расходы WB\" value={financeValue(ledgerSummary.expenses)} delta={\`логистика \${financeValue(ledgerSummary.logistics)}\`} icon={CircleDollarSign}/>\n        <MetricCard label=\"Удержания и штрафы\" value={financeValue(Number(ledgerSummary.penalties || 0)+Number(ledgerSummary.deductions || 0))} delta={\`\${formatNumber(ledgerSummary.movements || 0)} движений\`} icon={AlertTriangle}/>\n        <MetricCard label=\"Компенсации\" value={financeValue(ledgerSummary.compensations)} delta={\`FBS-логистика \${financeValue(ledgerSummary.fbsLogistics)}\`} icon={TrendingUp}/>` ,
`        <MetricCard label=\"К перечислению\" value={financeValue(headlineSellerPayable)} delta={headlineSellerPayable == null ? 'ожидает поле forPay из реестра WB за выбранный период' : 'поле forPay из отчёта WB · выбранный период'} icon={WalletCards}/>\n        <MetricCard label=\"Доступно к выводу\" value={financeBalance?.for_withdraw == null ? 'Ожидается' : formatMoney(financeBalance.for_withdraw)} delta=\"текущий снимок WB · не зависит от выбранного периода\" icon={WalletCards}/>\n        <MetricCard label=\"Баланс WB\" value={financeBalance?.current == null ? 'Ожидается' : formatMoney(financeBalance.current)} delta={financeBalance?.updatedAt ? \`обновлено \${formatLocalDateTime(financeBalance.updatedAt)}\` : financeAnalyticsPeriodAligned ? 'ночной снимок ещё не загружен' : 'ждём актуальное чтение; старый баланс скрыт'} icon={CircleDollarSign}/>\n        <MetricCard label=\"Расходы WB\" value={financeValue(headlineExpenses)} delta={financeEstimateAvailable && !ledgerHasMovements ? \`предварительный расчёт · логистика \${financeValue(headlineLogistics)}\` : \`логистика \${financeValue(headlineLogistics)}\`} icon={CircleDollarSign}/>\n        <MetricCard label=\"Удержания и штрафы\" value={financeValue(headlineRetentions)} delta={ledgerHasMovements ? \`\${formatNumber(ledgerSummary.movements || 0)} движений\${financePartial ? ' · частично' : ''}\` : 'реестр выбранного периода ожидается'} icon={AlertTriangle}/>\n        <MetricCard label=\"Компенсации\" value={financeValue(headlineCompensations)} delta={ledgerHasMovements ? \`FBS-логистика \${financeValue(ledgerSummary.fbsLogistics)}\` : 'реестр выбранного периода ожидается'} icon={TrendingUp}/>` ,
'finance headline cards')

fs.writeFileSync(file,source)
console.log('ELISEI 5.19.14 finance selected-period integrity UI applied')
