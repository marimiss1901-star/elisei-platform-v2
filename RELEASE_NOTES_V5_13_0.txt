ELISEI 5.13.0 / backend 2.25.0 / El Core 5.13.0 — DAILY READY

- Главная по умолчанию показывает «Вчера».
- Готовый снимок вчерашнего дня хранится в PostgreSQL (`wb_daily_snapshots`).
- Вход пользователя не запускает WB API; UI читает сохранённый снимок.
- Заказы/продажи обновляются автоматически примерно раз в 30 минут, реклама/остатки примерно раз в час; остальные оперативные потоки реже.
- Финансы/хранение/приёмка/эквайринг работают отдельной тяжёлой полосой и уважают Smart Scheduler / next_allowed_at.
- Неподтверждённые нули скрываются: ready / partial / waiting / missing.
- Ручной запуск перенесён в диагностический сценарий.
- Для текущего Free Render добавлен GitHub Actions wake-up: 05:00, 07:30 и 11:30 МСК. Wake только поднимает backend; реальные API-запросы решает Smart Scheduler.
- Password Reset hotfix сохранён в backend/API при наложении патча.
- Regression suite: 39/39 команд прошли.
- Backend/JS syntax, JSX parse, YAML parse проверены.
