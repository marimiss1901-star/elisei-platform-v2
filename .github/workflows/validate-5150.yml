name: Validate ELISEI main

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Frontend install
        run: npm install
      - name: Frontend build
        run: npm run build
      - name: Backend install
        working-directory: backend
        run: npm install
      - name: Backend regression suite
        working-directory: backend
        run: npm test
